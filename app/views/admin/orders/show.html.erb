ここは注文履歴詳細画面だよ


購入者
<%= @order.name%>
注文日
<%= @order.created_at %>

配送先
<%= @order.shipping_address %>
<%= @order.shipping_number %>
<%= @order.shipping_name %>

支払方法


注文ステータス
<%= form_with model: @orders ,url: admin_orders_path, method: :patch do |f| %>
<%= f.select :payment_status,{'入金待ち':0,'入金確認':1,'製作中':2, '発送準備中':3,'発送済み':4} %>
<% end %>

一覧
<%= @order_item.each do |f| %>
商品名
<%= f.name %>
単価(税込)
<%= f.%>
数量
<%= f.order_item.ammount %>
小計

製作ステータス